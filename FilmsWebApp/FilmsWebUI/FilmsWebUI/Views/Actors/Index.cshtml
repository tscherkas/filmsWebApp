@{
    ViewBag.Title = "Actors Listing";
}
@using PagedList.Mvc; 
@using PagedList; 

<!-- import the included stylesheet for some (very basic) default styling 
<link href="/Content/PagedList.css" rel="stylesheet" type="text/css" />-->

@Html.TextBox("CurrentFilter")

@Html.Partial("ActorsList", (IPagedList)ViewBag.OnePageOfProducts)
@Html.PagedListPager((IPagedList)ViewBag.OnePageOfProducts, page => Url.Action("Index", new { page, currentFilter = ViewBag.CurrentFilter }))